@using AfricanIdeas.Services
@inherits LayoutComponentBase
@inject NavigationManager Nav
@inject AuthService Auth

<nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
    @* <div class="container-fluid">
        <a class="navbar-brand" href="#">Course App</a>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
                <a class="nav-link" @onclick="@(() => Nav.NavigateTo("/login"))">Login</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" @onclick="@(() => Nav.NavigateTo("/register"))">Register</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" @onclick="@(() => Nav.NavigateTo("/courses"))">Courses</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" @onclick="@(() => Nav.NavigateTo("/mycourses"))">My Courses</a>
            </li>
        </ul>
    </div> *@
</nav>


<div class="container d-flex justify-content-center align-items-start" style="min-height: 80vh; padding-top: 30px;">
    <div class="w-75">
        @Body
    </div>
</div>

@code {
    void NavigateTo(string url)
    {
        Nav.NavigateTo(url);
    }
}
